# Raspberry Pi / Linux (Phase 2) deps. Install with:
#   pip install -r requirements-common.txt -r requirements-pi.txt
#
# TTS: install the `piper-tts` binary (preferred) or `espeak-ng` as a fallback,
#      plus ALSA's `aplay`, via apt — these are system packages, not pip:
#   sudo apt install espeak-ng alsa-utils
#   (piper: see https://github.com/rhasspy/piper for the binary + a voice model)
#
# Set JARVIS_PIPER_MODEL to your downloaded .onnx voice when using piper.
#
# Offline LLM (Phase 3.5): install Ollama as a system service and pull a model,
# then set JARVIS_LLM_BACKEND=ollama (or leave "auto" for Claude-with-fallback):
#   curl -fsSL https://ollama.com/install.sh | sh
#   ollama pull llama3.1
#   (the Ollama backend uses stdlib HTTP — no extra pip package needed)
-r requirements-common.txt
pynput            # push-to-talk listener (Mac; needs a keyboard)
openwakeword      # wake-word engine (default) — keyless, offline, "hey_jarvis"
# pvporcupine     # alt wake engine; lighter but needs Picovoice commercial approval
