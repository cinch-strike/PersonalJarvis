# Cross-platform Jarvis deps (Mac + Pi).
# Install with: pip install -r requirements-common.txt
faster-whisper
sounddevice
numpy
anthropic
boto3
ddgs            # keyless DuckDuckGo web-search tool (weather uses stdlib)
